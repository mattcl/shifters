platform: linux
image_resource:
  type: registry-image
  source:
    repository: rust
    tag: "1.70"

inputs:
  - name: repo

outputs:
  - name: version

run:
  path: /bin/sh
  args:
    - "-c"
    - |
      cat repo/Cargo.toml | grep version | head -1 | cut -d '"' -f 2 > version/version
